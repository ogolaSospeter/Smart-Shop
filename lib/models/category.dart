class Categories {
  int id;
  String name;
  String image;
  bool isSelected;

  Categories(
      {required this.id,
      required this.name,
      required this.image,
      required this.isSelected});
}
